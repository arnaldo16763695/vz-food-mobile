import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Keep all brand tokens here so palette changes do not leak across widgets.
  static const Color brandPrimary = Color(0xFF1B9C85);
  static const Color brandPrimaryDark = Color(0xFF147563);
  static const Color accentWarm = Color(0xFFFFB648);
  static const Color background = Color(0xFFF4F7F5);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF18211F);
  static const Color textMuted = Color(0xFF61706C);
  static const Color border = Color(0xFFD9E3DF);
}
